PROJECT_NAME=My Project (Playwright)
MOCK_DIR=./example/my-project/testMockData
PORT=5000
PREFERRED_SERVER_PORTS=[4051]
PLAYWRIGHT_DIR=../playwright/
FALLBACK_DIR=../build
# PROJECT_TYPE controls which recording features show in the Record section.
# Supported values: playwright (default) or react
PROJECT_TYPE=playwright